<div class="jr-heading">
  {%- if kicker != blank -%}
    <p class="jr-kicker">{{ kicker }}</p>
  {%- endif -%}
  <h2>{{ title }}</h2>
  {%- if description != blank -%}
    <p>{{ description }}</p>
  {%- endif -%}
</div>
