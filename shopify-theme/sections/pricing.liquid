{% render 'pricing', muted: section.settings.muted %}

{% schema %}
{
  "name": "Prijzen",
  "settings": [
    { "type": "checkbox", "id": "muted", "label": "Grijze achtergrond", "default": true }
  ]
}
{% endschema %}
